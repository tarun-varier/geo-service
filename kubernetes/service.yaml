apiVersion: v1
kind: Service
metadata:
  name: gps-service
  labels:
    app: gps-service
spec:
  type: NodePort
  selector:
    app: gps-service
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http
